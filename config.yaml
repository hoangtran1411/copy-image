# Configuration file for copy-image
# Source directory - Network path to copy files from
source: "\\\\192.1.1.1\\DM_DON_GIA_LUONG\\ROUTING 2023 + HÌNH MẪU\\HINHMAUSP\\HÌNH CHƯA TẢI"

# Destination directory - Network path to copy files to
destination: "\\\\192.1.1.20\\dmdgl$\\HinhAnh"

# Number of concurrent workers for parallel copying
workers: 10

# Whether to overwrite existing files
overwrite: true

# File extensions to include (empty = all files)
extensions:
  - .jpg
  - .jpeg
  - .png
  - .gif
  - .bmp
  - .webp

# Maximum retry attempts for failed copies
max_retries: 3

# Dry run mode - show what would be copied without actually copying
dry_run: false
